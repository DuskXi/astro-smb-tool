<Grid
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    RowSpacing="8" Padding="4">
  <Grid.RowDefinitions>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="*"/>
  </Grid.RowDefinitions>

  <!-- 当前连接设备状态卡(带心跳) -->
  <Border Grid.Row="0" CornerRadius="6" Padding="12,10" Margin="0,0,0,4"
          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
    <Grid ColumnSpacing="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBlock x:Name="HbDot" Grid.Column="0" Text="●" FontSize="20"
                 VerticalAlignment="Center" Foreground="#9E9E9E"/>
      <StackPanel Grid.Column="1" Spacing="1">
        <TextBlock x:Name="ConnTitle" Text="当前未连接" FontWeight="SemiBold" FontSize="14"/>
        <TextBlock x:Name="ConnDetail" Text="到「浏览」页或下方扫描结果连接设备后,这里显示实时状态"
                   FontSize="12" Opacity="0.7" TextWrapping="Wrap"/>
      </StackPanel>
      <StackPanel Grid.Column="2" HorizontalAlignment="Right">
        <TextBlock x:Name="HbLatency" Text="" FontSize="18" FontWeight="SemiBold"
                   HorizontalAlignment="Right"/>
        <TextBlock x:Name="HbTime" Text="" FontSize="11" Opacity="0.6"
                   HorizontalAlignment="Right"/>
      </StackPanel>
    </Grid>
  </Border>

  <TextBlock Grid.Row="1" Text="扫描局域网内的 SMB 设备(显示主机名与时延)" FontWeight="SemiBold"/>

  <Grid Grid.Row="2" ColumnSpacing="6">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <TextBlock Grid.Column="0" Text="网段" VerticalAlignment="Center"/>
    <TextBox x:Name="SubnetBox" Grid.Column="1" Width="150" PlaceholderText="192.168.1"/>
    <TextBlock Grid.Column="2" Text=".1-254" VerticalAlignment="Center" Opacity="0.7"/>
    <Button x:Name="ScanBtn" Grid.Column="3" Content="开始扫描"/>
    <ProgressRing x:Name="ScanRing" Grid.Column="4" Width="20" Height="20" IsActive="False"/>
    <TextBlock x:Name="StatusText" Grid.Column="5" Text="" VerticalAlignment="Center"
               Opacity="0.75" Margin="6,0,0,0" TextTrimming="CharacterEllipsis"/>
  </Grid>

  <ProgressBar x:Name="ScanProgress" Grid.Row="3" Minimum="0" Maximum="254" Value="0"/>

  <ListView x:Name="ResultList" Grid.Row="4" SelectionMode="None">
    <ListView.ItemContainerStyle>
      <Style TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="10,6,10,6"/>
      </Style>
    </ListView.ItemContainerStyle>
  </ListView>
</Grid>
